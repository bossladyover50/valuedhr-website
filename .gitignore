# Non-website files
*.zip
*.url
ValuedHR Social Media Post Metricool template

# Git automation config (contains token — keep private)
.github-token

# macOS
.DS_Store

# Claude Code local settings (personal, not shared)
.claude/settings.local.json
